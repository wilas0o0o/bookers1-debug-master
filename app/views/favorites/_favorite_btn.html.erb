<% if book.favorited_by(current_user) %>
  <%= link_to "", book_favorites_path(book), method: :delete, remote: true, class: "fas fa-heart heart-red" %>
<% else %>
  <%= link_to "", book_favorites_path(book), method: :post, remote: true, class: "fas fa-heart heart-blue" %>
<% end %>